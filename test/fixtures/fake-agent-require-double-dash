#!/usr/bin/env bash
set -euo pipefail
seen=0
for a in "$@"; do
  if [[ "$a" == "--" ]]; then
    seen=1
  fi
done
if [[ "$seen" != "1" ]]; then
  exit 9
fi
last="${@: -1}"
printf '{"result":"dash-ok:%s","usage":{"inputTokens":1,"outputTokens":1,"cacheReadTokens":0},"duration_ms":1000}\n' "$last"

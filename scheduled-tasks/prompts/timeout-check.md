Run a timeout check for the scheduled Cursor pipeline.

Requirements:
- Use shell for exactly one blocking command: `sleep 20`
- After the sleep finishes, reply with exactly one line: `timeout-check finished`
- Do not modify any files.

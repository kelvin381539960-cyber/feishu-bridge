Run a lock-contention check for the scheduled Cursor pipeline.

Requirements:
- Use shell for exactly one blocking command: `sleep 30`
- After the sleep finishes, reply with exactly one line: `lock-check finished`
- Do not modify any files.
